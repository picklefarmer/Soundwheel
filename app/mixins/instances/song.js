import Ember from 'ember';
import promiseFire from './functions/promiseFire';
import knotWork		from './functions/knotWork';

const Hash = Ember.RSVP.hash;

	promiseFire(_)

const song = {
		knot:['chords','instruments'],
		settings:['routes','panels','main'],
		songs:['destiny','mighty'],
	};

export default knotWork.call(this,song,promiseFire)
