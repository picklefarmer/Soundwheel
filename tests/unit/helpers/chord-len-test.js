import { chordLen } from 'sound-lab/helpers/chord-len';
import { module, test } from 'qunit';

module('Unit | Helper | chord len');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = chordLen([42]);
  assert.ok(result);
});
