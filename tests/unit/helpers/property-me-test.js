import { propertyMe } from 'sound-lab/helpers/property-me';
import { module, test } from 'qunit';

module('Unit | Helper | property me');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = propertyMe([42]);
  assert.ok(result);
});
