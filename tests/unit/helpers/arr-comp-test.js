import { arrComp } from 'sound-lab/helpers/arr-comp';
import { module, test } from 'qunit';

module('Unit | Helper | arr comp');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrComp([42]);
  assert.ok(result);
});
