import { lookUp } from 'sound-lab/helpers/look-up';
import { module, test } from 'qunit';

module('Unit | Helper | look up');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = lookUp([42]);
  assert.ok(result);
});
