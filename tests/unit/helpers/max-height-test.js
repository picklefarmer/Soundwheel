import { maxHeight } from 'sound-lab/helpers/max-height';
import { module, test } from 'qunit';

module('Unit | Helper | max height');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = maxHeight([42]);
  assert.ok(result);
});
