import { isClassy } from 'sound-lab/helpers/is-classy';
import { module, test } from 'qunit';

module('Unit | Helper | is classy');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isClassy([42]);
  assert.ok(result);
});
