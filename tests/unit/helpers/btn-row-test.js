import { btnRow } from 'sound-lab/helpers/btn-row';
import { module, test } from 'qunit';

module('Unit | Helper | btn row');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = btnRow([42]);
  assert.ok(result);
});
