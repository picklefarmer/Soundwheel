import { btnOut } from 'sound-lab/helpers/btn-out';
import { module, test } from 'qunit';

module('Unit | Helper | btn out');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = btnOut([42]);
  assert.ok(result);
});
